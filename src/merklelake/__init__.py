"""
MerkleLake — Week 1 core (skeleton)

This package ships the *interfaces* and *contracts* for:
    - merkle tree construction + proofs (src/merklelake/proofs/merkle.py)
    - block headers + link-hash chaining (src/merklelake/proofs/chain.py)
    - in-memory sealing of events into a block (src/merklelake/seal.py)
"""
